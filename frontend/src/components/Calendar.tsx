/**
 * Spec: la UI vive en `pages/Turnos.tsx`; exports útiles para revisión / tests.
 */
export { formatFecha, formatHora } from '../utils/dateFieldFormat';
export { isSelectableSlotAction, type CalendarSlotInfo } from '../utils/calendarSlotSelection';
