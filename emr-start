#!/usr/bin/env bash
# Arranque local EMR — stack Docker (db + backend).
# Ver docs/dev-start.md y ./emrctl help

set -euo pipefail

BASE_DIR="$(cd "$(dirname "$0")" && pwd)"

SEED_FLAG=""
if [ "${RUN_SEED:-false}" = "true" ]; then
  SEED_FLAG="--seed"
fi

exec "$BASE_DIR/emrctl" up $SEED_FLAG
