Django>=5.0
djangorestframework
django-cors-headers
django-filter
psycopg2-binary
python-dotenv
djangorestframework_simplejwt
Faker
requests
reportlab>=4.0
python-barcode>=0.15
Pillow>=10.0
openpyxl>=3.1
gunicorn>=22,<24

# --- Desarrollo / tests (no requeridos en runtime productivo mínimo) ---
# Instalar solo en entornos de desarrollo/CI: pip install -r requirements.txt
pytest
pytest-django